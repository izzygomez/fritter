<!-- deprecated for now, but I might bring the functionality back, so that's why this file still exits -->
<div data-tweet-id="{{id}}">
  <input type="text" value="{{existingText}}" />
  <button class="submit-button">Submit</button>
  <button class="cancel-button">Cancel</button>
</div>
